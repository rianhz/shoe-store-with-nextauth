export const metadata = {
	title: "Contact | Shoe Store",
	description: "Contact of Shoe Store",
};

export default function ProductLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
