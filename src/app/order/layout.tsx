export const metadata = {
	title: "Order | Shoe Store",
	description: "Order of Shoe Store",
};

export default function ProductLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
