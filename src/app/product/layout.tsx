export const metadata = {
	title: "Product | Shoe Store",
	description: "Product of Shoe Store",
};

export default function ProductLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
